# React GraphQL UI Generator

Generates React interfaces starting from GraphQL

## Main goals

- [ ] Render interface according to a GraphQL schema
- [ ] Display data returned by a GraphQL query
- [ ] Edit and update the data
